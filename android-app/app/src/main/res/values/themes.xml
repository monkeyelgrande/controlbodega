<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Tema CLARO fijo (no DayNight): el design system de Stitch es light.
         Esto evita el "texto oscuro sobre fondo oscuro" en celulares en
         modo oscuro. -->
    <style name="Theme.BodegaMovil" parent="Theme.Material3.Light.NoActionBar">
        <item name="colorPrimary">@color/primary</item>
        <item name="colorOnPrimary">@color/on_primary</item>
        <item name="colorPrimaryContainer">@color/primary_container</item>
        <item name="colorOnPrimaryContainer">@color/on_primary_container</item>
        <item name="colorSurface">@color/surface_card</item>
        <item name="colorOnSurface">@color/on_surface</item>
        <item name="android:colorBackground">@color/surface</item>
        <item name="colorError">@color/error</item>
        <item name="colorOnError">@color/on_error</item>
        <item name="android:statusBarColor">@color/primary_dark</item>
        <item name="android:windowLightStatusBar" tools:targetApi="m">false</item>
    </style>

    <!-- Boton principal: alto 56dp, texto 16sp, sin MAYUSCULAS -->
    <style name="Widget.Bodega.Button" parent="Widget.Material3.Button">
        <item name="android:minHeight">56dp</item>
        <item name="android:textSize">16sp</item>
        <item name="android:textStyle">bold</item>
        <item name="android:letterSpacing">0</item>
        <item name="textAllCaps">false</item>
        <item name="cornerRadius">8dp</item>
        <item name="backgroundTint">@color/primary</item>
        <item name="android:textColor">@color/on_primary</item>
    </style>

    <!-- Boton secundario (azul accion) -->
    <style name="Widget.Bodega.Button.Azul" parent="Widget.Bodega.Button">
        <item name="backgroundTint">@color/accion_azul</item>
        <item name="android:textColor">@color/on_accion</item>
    </style>

    <!-- Boton contorno (acciones suaves) -->
    <style name="Widget.Bodega.Button.Outline" parent="Widget.Material3.Button.OutlinedButton">
        <item name="android:minHeight">56dp</item>
        <item name="android:textSize">16sp</item>
        <item name="android:textStyle">bold</item>
        <item name="textAllCaps">false</item>
        <item name="cornerRadius">8dp</item>
        <item name="strokeColor">@color/primary</item>
        <item name="android:textColor">@color/primary</item>
    </style>

    <style name="Widget.Bodega.Input" parent="Widget.Material3.TextInputLayout.OutlinedBox">
        <item name="boxStrokeColor">@color/primary</item>
        <item name="hintTextColor">@color/on_surface_variant</item>
    </style>
</resources>
